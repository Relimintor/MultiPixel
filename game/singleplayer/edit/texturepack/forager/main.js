(function () {
  window.SingleplayerTexturePackRegistry?.register?.({
    id: 'forager',
    name: 'Forager',
    description: 'Starter texture-pack slot. Add overrides in this folder to customize assets.',
    assetOverrides: {
      // Example format:
      // GRASS: 'textures/grass.png'
    }
  });
})();
